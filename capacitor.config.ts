import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '06-lector-qr',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
